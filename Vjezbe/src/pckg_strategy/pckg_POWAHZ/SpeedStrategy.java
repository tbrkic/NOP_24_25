package pckg_strategy.pckg_POWAHZ;

public class SpeedStrategy implements PowahStrategy{
	@Override
	public void applyPOWAH ( ) {
		System.out.println ("This person is slightly better than a noob because they're ever so slightly faster than the perfectly average human being." );
	}
}
